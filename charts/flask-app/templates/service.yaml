apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-flask-app
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
